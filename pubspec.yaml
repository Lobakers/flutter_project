name: beewhere
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
    

  cupertino_icons: ^1.0.8
  http: ^1.1.0  
  provider: ^6.0.5
  geolocator: ^14.0.2
  geocoding: ^2.0.5
  device_info_plus: ^12.0.2
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/background_login.png
    - assets/images/beeWhere.png

  uses-material-design: true

